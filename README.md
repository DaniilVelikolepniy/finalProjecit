# Сервис бронирования отелей

## 📑 Оглавление

- [📌 Описание проекта](#-описание-проекта)
- [⚙️ Технологии](#️-технологии)
- [🧩 Функционал](#-функционал)
- [🖥️ Страницы системы](#️-страницы-системы)
- [🗄️ Структура базы данных](#️-структура-базы-данных)
- [🚀 Установка и запуск](#-установка-и-запуск)
- [🚀 Установка на XAMP](#-установка-и-запуск-с-xampp)

---

## 📌 Описание проекта

Учебный проект, выполненный в рамках финальной работы курса **PHP фреймворк Laravel**.  
Сервис представляет собой онлайн-платформу для бронирования отелей.

Администратор управляет отелями, номерами и удобствами через панель управления.  

---

## ⚙️ Технологии

- **PHP:** 8.2  
- **Laravel:** 12.x  
- **База данных:** MySQL  
- **Docker:** Laravel Sail  
- **ORM:** Eloquent  
- **Аутентификация:** встроенная система Laravel (bcrypt для паролей)  
- **Почтовые уведомления:** Laravel Mail  

---

## 🧩 Функционал

### Незарегистрированные пользователи

- Регистрация.  

### Зарегистрированные пользователи

- Авторизация.  
- Просмотр списка отелей.  
- Просмотр карточки отеля и номеров.  
- Бронирование номеров.  
- Просмотр списка собственных бронирований.  
- Отмена бронирований.  
- Получение email-подтверждений.  

### Редактор

- Редактирование и удаление отелей.  
- Создание, редактирование и удаление комнат.  
- Добавление удобств отелю и комнатам.  
- Просмотр пользователей (из числа посетителей отеля) и их бронирований.  
- Удаление информации о пользователе.  

### Администратор

- Создание, редактирование и удаление отелей.  
- Добавление удобств в приложение.  
- Просмотр всех бронирований и пользователей.  
- Управление ролями пользователей.  
- Закрепление редактора за отелем.  
- Удаление информации о пользователе.  

---

## 🖥️ Страницы системы

- `/hotels` — список отелей.  
- `/hotels/{id}` — страница отеля с доступными номерами и формой бронирования.  
- `/bookings` — список бронирований пользователя.  
- `/booking/{id}` — подробности бронирования.  
- `/register` — регистрация.  
- `/login` — авторизация.  

---

## 🗄️ Структура базы данных

Основные таблицы:

- `users` — пользователи.  
- `hotels` — отели.  
- `rooms` — номера.  
- `bookings` — бронирования.  
- `facilities` — удобства.  
- `facility_hotel`, `facility_room` — связи многие-ко-многим.  

---

## 🚀 Установка и запуск

Все команды выполняются в **корне проекта**.

### Создадим файл .env

```bash
cp .env.example .env
```

### Запускаем приложение Docker

- ***На Linux***

```bash
sudo systemctl start docker
```

- ***macOS***

```bash
open -a Docker
```

- ***Windows***

```bash
Start-Process "<путь_до_директории_с_файлом>/Desktop.exe"
```

### Устанавливаем зависимости в проекте

```bash
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    laravelsail/php84-composer:latest \
    composer install --ignore-platform-reqs
```

### Развернём окружение

```bash
./vendor/bin/sail up -d
```

### Сгенерируем ключ приложения

```bash
./vendor/bin/sail artisan key:generate
```

### Выполним миграции

```bash
./vendor/bin/sail artisan migrate
```

### Сидируем базу данных

```bash
./vendor/bin/sail artisan db:seed
```

### Переходим в бразуер для дальнейшего взаимодействия с приложением

[Наш проект](http://localhost/)

### 🚀 Установка и запуск с XAMPP

#### Клонируем проект в папку

```\xampp\htdocs\myproject```

#### Откройте терминал / консоль в директории проекта (только если нет композера)

```bash
composer install
```

#### Скопируйте пример конфигурации

```bash
cp .env.example .env
```

*на Windwos копирование идёт с помощью команды:*

```cmd
copy .env.example .env
```

#### Генерируем ключ приложения

```bash
php artisan key:generate
```

#### Создаём базу данных через PHPMyAdmin

[PHPMyAdmin](http://localhost/phpmyadmin)

_для БД используем название_

```laravel```

#### Выполняем миграции

```bash
php artisan migrate
```

#### Выполняем сидеры

```bash
php artisan db:seed
```
